# Development Log

## 2026-09-10 — Chapter 01 Vertical Slice

### Step 1. 폐장 골목 전투 튜토리얼화

목표: 첫 방에서 설명을 읽는 것보다 실제 입력을 순서대로 사용하면서 핵심 조작을 익히게 한다.

적용 내용:
- 이동 → 점프 → 기본 공격 → 대시 → 익절 순서의 5단계 튜토리얼 추가
- 이동은 시작 지점에서 90px 이상 이동하면 완료 처리
- 점프는 C, 기본 공격은 X, 대시는 Z 입력 시 다음 단계로 진행
- 익절은 미실현 수익 25 이상이며 재사용 대기시간이 끝난 상태에서 실제로 사용해야 완료
- 튜토리얼은 전투를 막지 않으며, 플레이어가 적을 먼저 처치해도 기존 진행 흐름은 유지
- 각 단계 완료를 `tutorial_step_complete`, 전체 완료를 `tutorial_complete` 플레이 로그에 기록
- 순차 진행과 익절 조건을 회귀 테스트에 추가

### Step 2. 손절 교차로 적 조합 및 회피 학습 강화

목표: 두 번째 방에서 단순 적 수 증가가 아니라, 근접 → 폭발 예고 → 원거리 조준으로 위협이 단계적으로 겹치며 회피 판단을 배우게 한다.

적용 내용:
- 기존 배치를 유지해 앞쪽 잡주, 중간 폭탄, 후반 공매도 유령이 순차적으로 활성화되는 흐름을 활용
- 폭탄이 `windup/charge` 상태가 되면 C 점프 또는 Z 대시로 범위 이탈을 유도하는 안내 추가
- 폭탄 예고 중 실제 점프/대시 대응을 `hazard_response(threat=bomb)` 로그로 기록
- 공매도 유령이 조준 상태에 들어가면 발사 전에 위치를 바꾸도록 안내
- 유령 조준 또는 근접 투사체 상황에서 점프/대시 대응을 `hazard_response(threat=ghost)` 로그로 기록
- 폭탄과 유령 위협 제거 시 `crossroads_training_complete` 로그 기록
- 회피 학습 성공 여부가 방 클리어 조건을 막지 않도록 구성하여 강제 튜토리얼이 되지 않게 함
- 폭탄 대응, 유령 대응, 학습 완료에 대한 회귀 테스트 추가

### Step 3. 공칠의 거래소 선택 고민 강화

목표: 상점을 단순한 구매 화면이 아니라, 남은 4·5번 방을 대비해 공격력·생존력·시드 보존을 비교하는 결정 지점으로 만든다.

적용 내용:
- 1장 거래소에서는 종목을 최대 1개만 신규 편입하도록 제한
- 성장주/배당주/가치주 중 하나를 고른 뒤 다른 종목을 추가 구매할 수 없게 하여 선택의 무게 강화
- 체력 회복은 종목 구매와 별개로 최대 1회만 허용
- 종목 구매 이후 남은 시드를 회복에 쓸지 다음 구역까지 보존할지 선택 가능
- 종목 구매를 `shop_purchase_decision` 로그로 기록하고 비용, 구매 후 시드, 구매 전후 HP 저장
- 상점 종료 시 `shop_exit_decision`에 선택 종목, 회복 여부, 남은 시드, HP 기록
- 종목 중복 구매 제한, 회복 1회 제한, 상점 종료 상태에 대한 회귀 테스트 추가

### Step 4. 폐쇄 거래소 엘리트/탐험 보상 강화

목표: 4번 방에서 상층 탐험과 선택형 엘리트 도전을 실제 위험-보상 판단으로 만든다.

적용 내용:
- 상층 보관함 360px 이내 접근 시 선택형 엘리트 도전의 위험/보상 안내 추가
- 엘리트 도전 수락/거절을 `elite_route_decision` 로그로 기록
- 도전 시작 HP를 저장해 성과 평가에 사용
- 기본 엘리트 보상 외에 피해량 기반 성과 시드 추가
  - 무피해: +20 시드
  - 피해 20 이하: +10 시드
  - 피해 20 초과: 추가 보너스 없음
- 완료 시 `elite_trial_performance`에 받은 피해와 추가 시드 기록
- 도전을 거절해도 기존 출구/진행은 막히지 않도록 유지
- 수락/거절 및 무피해 성과 보너스 회귀 테스트 추가

### Step 5. 작전세력 소굴 보스전 연출 및 패턴 완성

목표: 마지막 방을 단순 고체력 적이 아니라, 1장에서 익힌 이동·점프·대시·위험 예고 판독을 종합 검증하는 보스전으로 만든다.

적용 내용:
- 보스 입장 시 `boss_encounter_start` 로그와 상장 심사 안내 추가
- 체력 50% 이하에서 PHASE 02 전환 연출 추가
  - 기존 투사체와 바닥 위험 제거
  - 짧은 정지 연출
  - `boss_phase_change` 로그 기록
- 체력 20% 이하에서 1회성 `호가창 붕괴` 최후 패턴 추가
  - 넓은 바닥 위험을 생성하되 플레이어 위치 기준 안전 틈을 남김
  - 높은 발판 또는 안전 틈으로 회피 가능
  - `boss_desperation_pattern` 로그 기록
- 보스 처치 시 전투 시간, 시작/종료 HP, 최저 HP, 총 HP 손실을 `boss_clear_performance`로 기록
- 보스 2페이즈, 최후 패턴, 클리어 상태 회귀 테스트 추가

### Step 6. 1장 클리어 결과 화면 폴리싱

목표: 기존 3장 완주용 결과 문구를 제거하고 `잡주의 골목` Vertical Slice 한 판을 완주했다는 종료감을 명확하게 만든다.

적용 내용:
- 결과 헤더를 `CHAPTER 01 CLEAR · 상장 승인`으로 변경
- 승격 결과를 `PENNY STOCK → SMALL CAP`으로 고정
- `잡주의 골목 5 / 5 구역 완료` 안내 추가
- 전투 · 회피 · 상점 · 탐험 · 엘리트 · 보스까지 1장 핵심 루프를 완주했음을 결과 화면에 표시
- 재도전 버튼을 `잡주의 골목 다시 도전`으로 수정
- 기존 2장 시작 버튼은 Vertical Slice 모드에서 숨김 유지

## 현재 Vertical Slice 상태

플레이 흐름:
`폐장 골목 → 손절 교차로 → 공칠의 거래소 → 폐쇄 거래소 → 작전세력 소굴 → SMALL CAP 승격`

현재 1장 핵심 루프의 기능 고도화 1차 사이클은 완료했습니다. 다음 사이클은 실제 플레이 피드백을 기준으로 전투 시간, 회피 성공률, 상점 선택 분포, 엘리트 도전 선택률, 보스 피해/클리어 시간을 보고 밸런싱하는 단계입니다.
